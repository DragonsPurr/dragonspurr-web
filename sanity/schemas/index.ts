import { blogPost } from './blogPost';
import { brandType } from './brand';
import { contributor } from './contributor';
import { portfolioItem } from './portfolioItem';

export const schemaTypes = [contributor, blogPost, portfolioItem, brandType];
